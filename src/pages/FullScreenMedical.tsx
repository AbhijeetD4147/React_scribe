import { MedicalInterface } from "@/components/MedicalInterface";

const FullScreenMedical = () => {
  return (
    <div className="h-screen w-screen">
      <MedicalInterface />
    </div>
  );
};

export default FullScreenMedical;